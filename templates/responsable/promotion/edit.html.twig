{% extends 'base.html.twig' %}

{% block title %}Editer une Promotion{% endblock %}

{% block body %}
    <h1>Editer une Promotion</h1>

    {{ include('responsable/promotion/_form.html.twig', {'button_label': 'enregistrer'}) }}

    <div class="button-container">

        <a class="btn-action" href="{{ path('app_responsable_promotion_index') }}">retour</a>
    
        {{ include('responsable/promotion/_delete_form.html.twig') }}
    </div>
{% endblock %}
