<form method="post" action="{{ path('app_admin_responsable_territorial_delete', {'id': responsable_territorial.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ responsable_territorial.id) }}">
    <button class="btn">Delete</button>
</form>
