<form method="post" action="{{ path('app_admin_formateur_delete', {'id': formateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formateur.id) }}">
    <div class="button-container">
        <button class="btn-action" class="btn">Supprimer</button>
    </div>
</form>
