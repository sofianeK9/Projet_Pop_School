{% extends 'base.html.twig' %} {% block title %}Edition profil{% endblock %} {%
block body %}
<h1>Edition profil</h1>

<table class="table">
  <thead>
    <tr>
      <th>Email</th>
      <th>action</th>
      </thead>   
    </tr>
    <tbody>
      <tr> 
        <td>{{ user.email }}</td>
        <td>
          <div class="button-container">
            <a class="btn-action" href="{{ path('app_profile_index') }}">retour</a>
          
            <a class="btn-action" href="{{ path('app_profile_edit', { id: user.id }) }}"
              >éditer</a
            >
          
            {%if is_granted('ROLE_ADMIN')%}
            {{ include("profile/_delete_form.html.twig") }}
            {% endif %}
          </div>

        </td>
      </tr>
    </tbody>
  </table>

{% endblock %}
