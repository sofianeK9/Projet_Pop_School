{% extends 'base.html.twig' %}

{% block title %}Formateur{% endblock %}

{% block body %}
    <h1>Formateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ formateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ formateur.prenom }}</td>
            </tr>
        </tbody>
    </table>
    <div class="button-container">

        <a class="btn-action" href="{{ path('app_admin_formateur_index') }}">retour</a>
    
        <a class="btn-action" href="{{ path('app_admin_formateur_edit', {'id': formateur.id}) }}">éditer</a>
    
        {{ include('admin/formateur/_delete_form.html.twig') }}
    </div>
{% endblock %}
