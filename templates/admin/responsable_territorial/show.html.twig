{% extends 'base.html.twig' %}

{% block title %}Responsable Territorial{% endblock %}

{% block body %}
<main>
    <h1>Responsable Territorial</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ responsable_territorial.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ responsable_territorial.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ responsable_territorial.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_responsable_territorial_index') }}">retour à la liste</a>

    <a href="{{ path('app_admin_responsable_territorial_edit', {'id': responsable_territorial.id}) }}">éditer</a>

    {{ include('admin/responsable_territorial/_delete_form.html.twig') }}
</main>
{% endblock %}
