{% extends 'base.html.twig' %} {% block title %}Administrateur{% endblock %} {%
block body %}
<h1>Administrateur</h1>

<table class="table">
  <tbody>
    <tr>
      <th>Id</th>
      <td>{{ administrateur.id }}</td>
    </tr>
    <tr>
      <th>Nom</th>
      <td>{{ administrateur.nom }}</td>
    </tr>
    <tr>
      <th>Prenom</th>
      <td>{{ administrateur.prenom }}</td>
    </tr>
  </tbody>
</table>

<div class="button-container">
  <a class="btn-action" href="{{ path('app_admin_administrateur_index') }}"
    >Retour</a
  >
  <a
    class="btn-action"
    href="{{
      path('app_admin_administrateur_edit', { id: administrateur.id })
    }}"
    >Éditer</a
  >

  {{ include("admin/administrateur/_delete_form.html.twig") }}
</div>
{% endblock %}
